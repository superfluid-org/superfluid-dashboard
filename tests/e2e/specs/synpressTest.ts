describe("Synpress is a little bit annoying" , () => {
    //Add test cases with transactions here , since they have wrapped the whole Cypress setup
    //Can't really change Synpress to use the same setup as Cypress, e.g. Cucumber , code coverage plugins etc.
})