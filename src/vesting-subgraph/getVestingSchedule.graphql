query getVestingSchedule($id: ID!) {
  createVestingScheduleEvent(id: $id) {
    id
    cliffDate
    cliffTransferAmount
    endDate
    flowRate
    receiver
    sender
    startDate
    superToken
    timestamp
    transactionHash
  }
}